<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todo.mapper.TodoMapper">

    <insert id="insert" parameterType="com.example.todo.entity.Todo"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todo (user_id, title, status, create_time)
        VALUES (#{userId}, #{title}, #{status}, #{createTime})
    </insert>

    <select id="findByUserId" resultType="com.example.todo.entity.Todo">
        SELECT id, user_id, title, status, create_time
        FROM todo
        WHERE user_id = #{userId}
        ORDER BY create_time DESC, id DESC
    </select>

    <select id="findByIdAndUserId" resultType="com.example.todo.entity.Todo">
        SELECT id, user_id, title, status, create_time
        FROM todo
        WHERE id = #{id}
          AND user_id = #{userId}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM todo
        WHERE user_id = #{userId}
    </select>

    <select id="countByUserIdAndTitle" resultType="long">
        SELECT COUNT(*)
        FROM todo
        WHERE user_id = #{userId}
          AND title = #{title}
    </select>

    <update id="updateStatus">
        UPDATE todo
        SET status = #{status}
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>

    <delete id="deleteByIdAndUserId">
        DELETE FROM todo
        WHERE id = #{id}
          AND user_id = #{userId}
    </delete>
</mapper>
